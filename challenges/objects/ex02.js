const prompt = require('prompt-sync')();

let student = {
    name : 'asta',
    second_name : 'at',
    results : [10,12,15,11,13]
}
console.log(`Student : ${student.name} ${student.second_name}`); 
console.log("Liste of notes : " + student.results );

